[build]
builder = "nixpacks"

[deploy]
startCommand = "python scripts/rebuild_index.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/status"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
